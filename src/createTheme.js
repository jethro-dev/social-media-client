import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#667fff",
    },
    secondary: {
      main: "#fff",
    },
    badgeColor: {
      main: "#fa3e3e",
    },
  },
});

export default theme;
